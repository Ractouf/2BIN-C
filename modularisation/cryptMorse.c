#include <stdio.h>
#include <stdlib.h>

static char morse[8][8] = {};

static char* encryptMorse(char* msg) {
	
}

static char* decryptMorse(char* msg) {
	
}

char* encrypt (char* msg) {
   return encryptMorse(msg);
}

char* decrypt (char* msg) {
   return decryptMorse(msg);
}